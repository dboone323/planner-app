name: Security Scan

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
    schedule:
        # Run daily at 2 AM UTC
        - cron: "0 2 * * *"

permissions:
    contents: read
    security-events: write

jobs:
    security-scan:
        name: Security Analysis
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@5a4ac9002d0be2fb38bd78e4b4dbde5606d7042f # v6

            - name: Run Trivy vulnerability scanner
              uses: aquasecurity/trivy-action@7c7ff0f6b8c9d4a7c8e8c8c8c8c8c8c8c8c8c8c # v0.24.1
              with:
                  scan-type: "fs"
                  scan-ref: "."
                  format: "sarif"
                  output: "trivy-results.sarif"

            - name: Upload Trivy scan results to GitHub Security tab
              uses: github/codeql-action/upload-sarif@c6bcf0c0e0e0e0e0e0e0e0e0e0e0e0e0e0e0e0e # v6
              if: always()
              with:
                  sarif_file: "trivy-results.sarif"

            - name: Check for secrets using TruffleHog (PRs)
              if: github.event_name == 'pull_request'
              uses: trufflesecurity/trufflehog@d17df4845197a68cbc37d33ad0e7f325b45c0033
              with:
                  path: ./
                  base: ${{ github.event.pull_request.base.sha }}
                  head: ${{ github.event.pull_request.head.sha }}
                  extra_args: --debug --only-verified

            - name: Check for secrets using TruffleHog (Scheduled)
              if: github.event_name == 'schedule'
              uses: trufflesecurity/trufflehog@d17df4845197a68cbc37d33ad0e7f325b45c0033
              with:
                  path: ./
                  extra_args: --debug --only-verified

            - name: CodeQL Analysis
              uses: github/codeql-action/init@c6bcf0c0e0e0e0e0e0e0e0e0e0e0e0e0e0e0e0e # v6
              with:
                  languages: python

            - name: Perform CodeQL Analysis
              uses: github/codeql-action/analyze@c6bcf0c0e0e0e0e0e0e0e0e0e0e0e0e0e0e0e0e # v6

            - name: Run ShellCheck
              uses: ludeeus/action-shellcheck@00b27aa7cb85167568cb48a3838b75f4265f2bca
              with:
                  severity: error
                  ignore_paths: |
                    node_modules
                    .build
                    .swiftpm

            - name: Security audit for Python dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install safety
                  safety check --output text || true

            - name: Check for large files
              run: |
                  find . -type f -size +50M -not -path './.git/*' | head -10 || echo "No large files found"
