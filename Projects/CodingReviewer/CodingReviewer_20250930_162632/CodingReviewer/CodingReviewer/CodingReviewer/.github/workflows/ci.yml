# Generated by AI-Enhanced Automation
# Tue Sep 30 14:25:49 CDT 2025

Here's a comprehensive GitHub Actions CI/CD workflow for your Swift project:

```yaml
# .github/workflows/ci-cd.yml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
    paths-ignore:
      - '**.md'
      - '**.txt'
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 2 * * 1'  # Weekly security scan
  workflow_dispatch:

env:
  DEVELOPER_DIR: /Applications/Xcode_15.2.app/Contents/Developer
  FASTLANE_XCODEBUILD_SETTINGS_TIMEOUT: 120
  SWIFT_VERSION: "5.9"
  PROJECT_NAME: "CodingReviewer"
  SCHEME_NAME: "CodingReviewer"
  DESTINATION_IOS: "platform=iOS Simulator,OS=17.2,name=iPhone 15 Pro"
  DESTINATION_MACOS: "platform=macOS"

permissions:
  contents: read
  checks: write
  pull-requests: write
  security-events: write

jobs:
  # Security scanning job
  security-scan:
    name: 🔐 Security Scan
    runs-on: macos-14
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Swift
        uses: swift-actions/setup-swift@v1
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - name: Install Security Tools
        run: |
          brew install swiftlint
          brew install swiftformat
          brew tap anchore/syft && brew install syft
          brew tap github/gh && brew install gh

      - name: Run SwiftLint Security Rules
        run: |
          swiftlint lint --config .swiftlint.yml --strict --reporter checkstyle > swiftlint-report.xml || true

      - name: Upload SwiftLint Results
        uses: github/codeql-action/upload-sarif@v2
        if: always()
        with:
          sarif_file: swiftlint-report.xml
          category: swiftlint

      - name: Dependency Security Check
        run: |
          swift package show-dependencies --format json > dependencies.json
          # Add dependency vulnerability scanning here if using external tools

      - name: Generate SBOM
        run: |
          syft . -o spdx-json > bom.spdx.json

      - name: Upload SBOM
        uses: actions/upload-artifact@v3
        with:
          name: sbom
          path: bom.spdx.json

  # Code quality checks
  code-quality:
    name: 📝 Code Quality
    runs-on: macos-14
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Swift
        uses: swift-actions/setup-swift@v1
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - name: Cache Swift Packages
        uses: actions/cache@v3
        with:
          path: .build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      - name: Install SwiftFormat
        run: |
          brew install swiftformat
          swiftformat --version

      - name: Install SwiftLint
        run: |
          brew install swiftlint
          swiftlint version

      - name: Run SwiftFormat Check
        run: |
          swiftformat . --lint --verbose
        continue-on-error: false

      - name: Run SwiftLint
        run: |
          swiftlint lint --strict
        continue-on-error: false

      - name: Generate SwiftLint Report
        run: |
          swiftlint lint --reporter html > swiftlint_report.html || true

      - name: Upload SwiftLint Report
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: swiftlint-report
          path: swiftlint_report.html

  # Performance benchmarking
  performance-benchmark:
    name: 🚀 Performance Benchmark
    runs-on: macos-14
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_15.2.app
          xcodebuild -version

      - name: Cache Derived Data
        uses: actions/cache@v3
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-xcode-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-xcode-

      - name: Build for Benchmarking
        run: |
          xcodebuild build-for-testing \
            -project "${{ env.PROJECT_NAME }}.xcodeproj" \
            -scheme "${{ env.SCHEME_NAME }}" \
            -destination "${{ env.DESTINATION_MACOS }}" \
            -configuration Release \
            -derivedDataPath DerivedData \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Run Performance Tests
        run: |
          xcodebuild test-without-building \
            -project "${{ env.PROJECT_NAME }}.xcodeproj" \
            -scheme "${{ env.SCHEME_NAME }}" \
            -destination "${{ env.DESTINATION_MACOS }}" \
            -configuration Release \
            -derivedDataPath DerivedData \
            -enableCodeCoverage YES \
            -resultBundlePath PerformanceTestResults \
            -test-iterations 3 \
            -maximum-concurrent-test-simulator-destinations 1

      - name: Upload Performance Results
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: performance-results
          path: PerformanceTestResults

  # Build and test matrix
  build-and-test:
    name: 🛠️ Build & Test (${{ matrix.platform }} - ${{ matrix.xcode }})
    needs: [code-quality, security-scan]
    runs-on: macos-${{ matrix.macos }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: iOS
            macos: 14
            xcode: "15.2"
            destination: "platform=iOS Simulator,OS=17.2,name=iPhone 15 Pro"
          - platform: iOS
            macos: 14
            xcode: "15.2"
            destination: "platform=iOS Simulator,OS=17.0,name=iPhone 14"
          - platform: macOS
            macos: 14
            xcode: "15.2"
            destination: "platform=macOS"
          - platform: iOS
            macos: 13
            xcode: "15.0.1"
            destination: "platform=iOS Simulator,OS=17.0.1,name=iPhone 15"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Xcode Version
        run: |
          sudo xcode-select -s /Applications/Xcode_${{ matrix.xcode }}.app
          xcodebuild -version

      - name: Cache Swift Packages
        uses: actions/cache@v3
        with:
          path: |
            .build
            ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-spm-xcode${{ matrix.xcode }}-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-xcode${{ matrix.xcode }}-

      - name: Resolve Package Dependencies
        run: |
          xcodebuild -resolvePackageDependencies \
            -project "${{ env.PROJECT_NAME }}.xcodeproj" \
            -scheme "${{ env.SCHEME_NAME }}"

      - name: Build Project
        run: |
          xcodebuild build \
            -project "${{ env.PROJECT_NAME }}.xcodeproj" \
            -scheme "${{ env.SCHEME_NAME }}" \
            -destination "${{ matrix.destination }}" \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | xcbeautify

      - name: Run Unit Tests
        run: |
          xcodebuild test \
            -project "${{ env.PROJECT_NAME }}.xcodeproj" \
            -scheme "${{ env.SCHEME_NAME }}" \
            -destination "${{ matrix.destination }}" \
            -configuration Debug \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | xcbeautify

      - name: Generate Code Coverage
        run: |
          xcrun llvm-cov export -format="lcov" \
            DerivedData/Build/Intermediates.noindex/CodeCoverage/Coverage.profdata \
            -instr-profile DerivedData/Build/Intermediates.noindex/CodeCoverage/Coverage.profdata \
            DerivedData/Build/Intermediates.noindex/CodeCoverage/Products/Debug-${{ matrix.platform }}*/${{ env.SCHEME_NAME }}.app/${{ env.SCHEME_NAME }} \
            > coverage.lcov || echo "Coverage generation failed"

      - name: Upload Test Results
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: test-results-${{ matrix.platform }}-${{ matrix.macos }}-${{ matrix.xcode }}
          path: |
            TestResults
            coverage.lcov

      - name: Upload Coverage to Codecov
        uses: codecov/codecov-action@v3
        if: matrix.platform == 'macOS'
        with:
          file: coverage.lcov
          flags: unittests
          name: codecov-umbrella

  # AI-enhanced analysis (optional)
  ai-analysis:
    name: 🤖 AI Code Analysis
    needs: [build-and-test]
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Check Ollama Availability
        id: ollama_check
        run: |
          if command -v ollama &> /dev/null; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Ollama (if available)
        if: steps.ollama_check.outputs.available == 'true'
        run: |
          ollama pull codellama:7b-code
          ollama run codellama:7b-code &

      - name: AI Code Review
        if: steps.ollama_check.outputs.available == 'true'
        run: |
          # This is a placeholder - implement your AI analysis logic
          echo "AI analysis would run here"
          # Example: analyze code changes with LLM
          # python scripts/ai_code_review.py

      - name: Comment on PR
        if: steps.ollama_check.outputs.available == 'true' && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🤖 AI Code Analysis: Review completed. No critical issues found.'
            })

  # Artifact generation
  generate-artifacts:
    name: 📦 Generate Artifacts
    needs: [build-and-test]
    runs-on: macos-14
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_15.2.app
          xcodebuild -version

      - name: Build iOS App
        run: |
          xcodebuild archive \
            -project "${{ env.PROJECT_NAME }}.xcodeproj" \
            -scheme "${{ env.SCHEME_NAME }}" \
            -destination "${{ env.DESTINATION_IOS }}" \
            -archivePath "build/${{ env.PROJECT_NAME }}.xcarchive" \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Export iOS IPA
        run: |
          xcodebuild -exportArchive \
            -archivePath "build/${{ env.PROJECT_NAME }}.xcarchive" \
            -exportPath "build" \
            -exportOptionsPlist "ExportOptions.plist" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Build macOS App
        run: |
          xcodebuild archive \
            -project "${{ env.PROJECT_NAME }}.xcodeproj" \
            -scheme "${{ env.SCHEME_NAME }}" \
            -destination "${{ env.DESTINATION_MACOS }}" \
            -archivePath "build/${{ env.PROJECT_NAME }}-macOS.xcarchive" \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Create DMG for macOS
        run: |
          # Create a simple DMG (you might want to use a more sophisticated tool)
          hdiutil create -volname "${{ env.PROJECT_NAME }}" \
            -srcfolder "build/${{ env.PROJECT_NAME }}-macOS.xcarchive/Products/Applications" \
            -ov -format UDZO "build/${{ env.PROJECT_NAME }}.dmg"

      - name: Generate Documentation
        run: |
          # Generate documentation if you have a documentation target
          # xcodebuild docbuild -project "${{ env.PROJECT_NAME }}.xcodeproj" -scheme "${{ env.SCHEME_NAME }}"

      - name: Package Artifacts
        run: |
          mkdir -p artifacts
          cp -r build/*.app artifacts/ 2>/dev/null || true
          cp -r build/*.ipa artifacts/ 2>/dev/null || true
          cp -r build/*.dmg artifacts/ 2>/dev/null || true
          cp -r build/*.xcarchive artifacts/ 2>/dev/null || true

      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: app-artifacts
          path: artifacts/

  # Dependency updates
  dependency-update:
    name: 🔄 Dependency Updates
    runs-on: macos-14
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.DEPENDENCY_UPDATE_TOKEN }}

      - name: Setup Swift
        uses: swift-actions/setup-swift@v1
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - name: Update Dependencies
        run: |
          swift package update

      - name: Check for Changes
        id: changes
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.DEPENDENCY_UPDATE_TOKEN }}
          commit-message: "chore: update swift package dependencies"
          title: "Update Swift Package Dependencies"
          body: "Automated dependency update by GitHub Actions"
          branch: chore/update-dependencies
          delete-branch: true

  # Deployment preparation
  prepare-deployment:
    name: 🚀 Prepare Deployment
    needs: [generate-artifacts, performance-benchmark]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v3
        with:
          name: app-artifacts
          path: artifacts/

      - name: Create Release Notes
        run: |
          echo "# Release Notes" > release-notes.md
          echo "## Changes in this Release" >> release-notes.md
          echo "" >> release-notes.md
          git log --oneline -10 >> release-notes.md

      - name: Upload Release Assets
        uses: actions/upload-artifact@v3
        with:
          name: release-assets
          path: |
            artifacts/
            release-notes.md

      - name: Create GitHub Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/*.ipa
            artifacts/*.dmg
            artifacts/*.xcarchive
          body_path: release-notes.md
          tag_name: v${{ github.run_number }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Summary and notifications
  pipeline-summary:
    name: 📊 Pipeline Summary
    needs: [security-scan, code-quality, build-and-test, performance-benchmark, generate-artifacts]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate Summary
        run: |
          echo "## 🚀 CI/CD Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Security Scan | ${{ needs.security-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Code Quality | ${{ needs.code-quality.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build & Test | ${{ needs.build-and-test.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Performance | ${{ needs.performance-benchmark.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Artifacts | ${{ needs.generate-artifacts.result }} |" >> $GITHUB_STEP_SUMMARY

      - name: Send Slack Notification
        if: failure() && github.ref == 'refs/heads/main'
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          channel: '#ci-cd-notifications'
          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
          icon_emoji: ':rotating_light:'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send Email Notification
        if: failure() && github.ref == 'refs/heads/main'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "CI/CD Pipeline Failed - ${{ github.repository }}"
          body: |
            Build failed for repository: ${{ github.repository }}
            Commit: ${{ github.sha }}
            Branch: ${{ github.ref }}
            Job: ${{ github.job }}

            Please check the pipeline results.
          to: team@example.com
          from: ci-notifications@example.com
```

You'll also need these additional configuration files:

**.swiftlint.yml**
```yaml
# .swiftlint.yml
disabled_rules:
  - identifier_name
  - line_length
  - type_body_length
  - function_body_length

opt_in_rules:
  - array_init
  - attributes
  - closure_end_indentation
  - closure_spacing
  - collection_alignment
  - colon
  - comma
  - compiler_protocol_init
  - computed_accessors_order
  - conditional_returns_on_newline
  - contains_over_filter_count
  - contains_over_filter_is_empty
  - contains_over_first_not_nil
  - control_statement
  - convenience_type
  - cyclomatic_complexity
  - deployment_target
  - discarded_notification_center_observer
  - discouraged_direct_init
  - discouraged_object_literal
  - discouraged_optional_boolean
  - discouraged_optional_collection
  - duplicate_enum_cases
  - duplicate_imports
  - dynamic_inline
  - empty_collection_literal
  - empty_count
  - empty_enum_arguments
  - empty_parameters
  - empty_parentheses_with_trailing_closure
  - empty_string
  - empty_xctest_method
  - explicit_init
  - explicit_self
  - fallthrough
  - fatal_error_message
  - file_header
  - file_length
  - first_where
  - for_where
  - force_cast
  - force_try
  - force_unwrapping
  - function_default_parameter_at_end
  - function_parameter_count
  - generic_type_name
  - identical_operands
  - identifier_name
  - implicit_getter
  - implicit_return
  - implicitly_unwrapped_optional
  - inclusive_language
  - invalid_swiftlint_command
  - is_disjoint
  - joined_default_parameter
  - large_tuple
  - last_where
  - leading_whitespace
  - legacy_cggeometry_functions
  - legacy_constant
  - legacy_constructor
  - legacy_hashing
  - legacy_nsgeometry_functions
  - legacy_random
  - literal_expression_end_indentation
  - lower_acl_than_parent
  - mark
  - missing_docs
  - modifier_order
  - multiline_arguments
  - multiline_function_chains
  - multiline_literal_brackets
  - multiline_parameters
  - multiple_closures_with_trailing_closure
  - nesting
  - nimble_operator
  - no_extension_access_modifier
  - no_fallthrough_only
  - no_grouping_extension
  - no_space_in_method_call
  - notification_center_detachment
  - ns_number_init_as_function_reference
  - nsobject_prefer_isequal
  - number_separator
  - object_literal
  - opening_brace
  - operator_usage_whitespace
  - operator_whitespace
  - optional_enum_case_matching
  - orphaned_doc_comment
  - overridden_super_call
  - override_in_extension
  - pattern_matching_keywords
  - prefer_nimble
  - prefer_self_type_over_type_of_self
  - prefer_zero_over_explicit_init
  - prefixed_toplevel_constant
  - private_action
  - private_outlet
  - private_over_fileprivate
  - private_unit_test
  - prohibited_super_call
  - protocol_property_accessors_order
  - quick_discouraged_call
  - quick_discouraged_focused_test
  - quick_discouraged_pending_test
  - redundant_discardable_let
  - redundant_nil_coalescing
  - redundant_objc_attribute
  - redundant_optional_initialization
  - redundant_set_access_control
  - redundant_string_enum_value
  - redundant_type_annotation
  - redundant_void_return
  - required_enum_case
  - return_arrow_whitespace
  - shorthand_operator
  - single_test_class
  - sorted_first_last
  - sorted_imports
  - statement_position
  - static_operator
  - strict_fileprivate
  - strong_iboutlet
  - superfluous_disable_command
  - switch_case_alignment
  - switch_case_on_newline
  - syntactic_sugar
  - todo
  - toggle_bool
  - trailing_closure
  - trailing_comma
  - trailing_newline
  - trailing_semicolon
  - trailing_whitespace
  - type_name
  - unavailable_function
  - unneeded_break_in_switch
  - unneeded_parentheses_in_closure_argument
  - unowned_variable_capture
  - untyped_error_in_catch
  - unused_capture_list
  - unused_closure_parameter
  - unused_control_flow_label
  - unused_enumerated
  - unused_optional_binding
  - unused_setter_value
  - valid_ibinspectable
  - vertical_parameter_alignment
  - vertical_parameter_alignment_on_call
  - vertical_whitespace
  - vertical_whitespace_closing_braces
  - vertical_whitespace_opening_braces
  - void_return
  - weak_delegate
  - xct_specific_matcher
  - xctfail_message
  - yoda_condition

included:
  - Sources
  - Tests

excluded:
  - Carthage
  - Pods
  - .build

analyzer_rules:
  - explicit_self
  - unused_import

cyclomatic_complexity:
  warning: 10
  error: 20

file_length:
  warning: 400
  error: 1000

function_parameter_count:
  warning: 5
  error: 8

line_length:
  warning: 120
  error: 200

type_body_length:
  warning: 200
  error: 350

identifier_name:
  excluded:
    - id
    - URL
    - GlobalAPIKey
```

**.swiftformat**
```ini
# .swiftformat
--allman false
--binarygrouping none
--commas inline
--comments indent
--decimalgrouping none
--elseposition same-line
--empty void
--exponentcase lowercase
--exponentgrouping disabled
--fractiongrouping none
--guardelse same-line
--hexgrouping none
--hexliteralcase uppercase
--ifdef no-indent
--indent 4
--indentcase true
--indentstrings false
--linebreaks lf
--octalgrouping none
--operatorfunc spaced
--patternlet hoist
--ranges spaced
--self remove
--semicolons inline
--stripunusedargs always
--trimwhitespace always
--wraparguments before-first
--wrapcollections before-first
```

**ExportOptions.plist**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>method</key>
    <string>development</string>
    <key>teamID</key>
    <string>YOUR_TEAM_ID</string>
    <key>compileBitcode</key>
    <false/>
    <key>thinning</key>
    <string>&lt;none&gt;</string>
</dict>
</plist>
```

This comprehensive workflow includes:

1. **Multi-platform testing** on different macOS versions and Xcode versions
2. **Code quality checks** with SwiftLint and SwiftFormat
3. **Security scanning** with dependency analysis
4. **Performance benchmarking** with test iterations
5. **Artifact generation** for iOS and macOS apps
6. **AI-enhanced analysis** (optional, requires Ollama setup)
7. **Automated dependency updates**
8. **Comprehensive error handling** and notifications
9. **Coverage reporting** with Codecov integration
10. **Release preparation** with GitHub Releases

To use this workflow:

1. Place the YAML file in `.github/workflows/ci-cd.yml`
2. Add the configuration files to your repository root
3. Set up required secrets in GitHub repository settings
4. Customize the project-specific variables at the top
5. Adjust the scheme and project names to match your project

The workflow handles failures gracefully and provides detailed reporting through GitHub's native features and external integrations.
